# Server Configuration
PORT=8000
HOST=0.0.0.0
ENVIRONMENT=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Database Configuration

# Supabase PostgreSQL (Required for persistence)
# Get these from: https://app.supabase.com/project/_/settings/api
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_KEY=your-supabase-anon-key

# Neo4j Aura (Optional - for skills graph)
# Get these from: https://console.neo4j.io
NEO4J_URI=neo4j+s://your-instance.databases.neo4j.io
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your-neo4j-password

# Upstash Redis (Optional - for caching)
# Get this from: https://console.upstash.com
REDIS_URL=redis://default:your-password@your-endpoint.upstash.io:6379

# Google Gemini API (REQUIRED for roadmap generation)
# Get from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your-google-api-key

# Auth
SECRET_KEY=your-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# NOTES:
# - Supabase MCP integration requires both SUPABASE_URL and SUPABASE_KEY
# - Without these, the backend will run in mock mode (in-memory storage)
# - GOOGLE_API_KEY is required for roadmap generation to work
# - Neo4j and Redis are optional enhancements
